import * as React from 'react';
import { ContainerRoute, DesignConfig } from './models';
interface MainProps {
    routes?: ContainerRoute[];
    designConfig?: DesignConfig;
}
declare const Main: React.FunctionComponent<MainProps>;
export default Main;
